FROM caddy:2-alpine

COPY Caddyfile /etc/caddy/Caddyfile

# A imagem oficial já roda:
#   caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
# O Caddy lê $PORT do ambiente (ver a diretiva de site no Caddyfile).
